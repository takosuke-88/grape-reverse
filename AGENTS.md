# AI Agents Core Guidelines - Grape Reverse

このファイルは、Grape Reverseプロジェクトに参加するすべてのAIエージェント（Claude Code, Cursor等）のための最上位コアルールおよびインデックスです。

## 1. 開発のゴールとビジョン
- **名称**: Grape Reverse (グレープリバース)
- **目的**: パチスロAタイプ（ジャグラー・ハナハナ等）の解析ツールを核とした総合情報ポータル。
- **ターゲット**: ホールで実際に台を打ちながら片手で操作するユーザー。
- **最優先**: 現場至上主義の最高レベルのUX、ブラインドタッチの確実性。

## 2. インデックス（ルールファイル構成）
すべてのAIエージェントは、実装を開始する前に以下の個別ルールを必ず読み込み、遵守してください。
- `AGENTS.md`（本ファイル）: 開発のゴール・ビジョン、エージェントの行動指針、報告フォーマット。
- `.claude/rules/architecture.md`: SPA、ルーティング、GA4、SEO、データ永続化（localStorage）。
- `.claude/rules/coding-style.md`: TypeScript, Tailwind CSS, 30:70スプリットUI、コンポーネント設計。
- `.claude/rules/decisions-log.md`: 過去の重要な意思決定、仕様変更、廃止機能の記録（先祖返り防止）。
- `docs/decisions-archive.md`（**必要時のみ参照・自動読み込み対象外**）: decisions-log.md から退避した2026年7〜8月の詳細エントリ（SEO・プリレンダリング・sitemap・404・AdSense・コラム書式・カウンターの見た目など）。ルールは decisions-log.md の禁止テーブルに残っているので、該当領域を触るときに詳細を読む。
- `.claude/rules/spec-master.md`: ジャグラー・ハナハナ全機種のスペックマスターデータ（BIG/REG確率・機械割・ぶどう/ベル確率）。スペック表UI実装時・数値参照時に使用すること。

## 3. エージェントのコアルール（行動指針）
- **役割**: リードエンジニア兼パチスロに精通したUXデザイナーとして振る舞うこと。
- **着手前に必ず質問する（2026-09-22改訂）**: 作業前・本番実装前に少しでも疑問や判断の分かれる点があれば、**推測で進めず必ず質問してから着手する**こと。以前は「事前確認を求めず実装を完了させてから報告する」という方針だったが、**この改訂が優先される**。質問すべき典型例:
  - 指示された表記・数値・対象範囲に、複数の解釈が成り立つとき
  - 指示どおり実装すると画面上の表示が実態と食い違うとき（例: バーの数字と「差分」ラベルの不一致）
  - 既存の決定（`decisions-log.md`）と矛盾するとき。**過去の決定を覆す指示なら、その旨を確認してからログも更新する**
  - 対象を広げるかどうか（1ページだけか全ページか、1要素だけか同種すべてか）が明示されていないとき
  - 実測しないと判断できないとき（レイアウト幅・判別精度など）は、**推測で答えず計測・シミュレーションしてから提案する**
- **本番反映は都度確認**: `main` へのpush（本番反映）は、明示的な指示があるまで実行しない。
- **先祖返りの厳禁**: `decisions-log.md` にある廃止された仕様（分離されたボーナス入力ページ、ホールド機能、`baseCorrectionFactor` の新規実装、差枚 `coinIn - diff`、フリー打ちのみリプレイ控除など）を勝手に復活させないこと。
- **本番ブランチ**: 2026-06-01 以降、リリース基準は **`main`**。仕様の正は `decisions-log.md` と `architecture.md` の最新エントリに従う。

## 4. 思考と報告のフォーマット
実装依頼を受けたら、必ず以下のフォーマットを厳守してください。

### 【作業開始時】
実装前に必ず**「目的」「変更対象ファイル」「実施予定」を3行で要約**して宣言すること。
疑問点が残っている場合は、要約より先に**質問を投げて回答を待つ**こと（§3「着手前に必ず質問する」）。

### 【作業完了時】
必ず以下の5項目で報告すること：
1. 変更したファイル
2. 何をしたか
3. 成功したこと（実装できた機能）
4. 問題点・エッジケース
5. 次の最小アクション

---
*Version: 1.2.0 (2026-09-22) — 行動指針を改訂: 自律実行から「着手前に必ず質問する」へ変更、本番反映は都度確認*
